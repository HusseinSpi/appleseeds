let name = "hussein";
let age = "21";

console.log(`Hello, My name is ${name} and I am ${age} years old..`);

const length = 5;
const width = 3;
let area = length * width;

console.log(
  `The area of a rectangle with length of ${length} and width of ${width} is ${area}`
);

const today = "sunday";
const task1 = "watch movies";
const task2 = "eat";
const task3 = "sleep";

console.log(`On ${today}, I will ${task1}, ${task2}, and ${task3}`);

const myPets = 0;

console.log(`I have ${myPets} peats`);

const base = 21;
const height = 30;
area = (base * height) / 2;

console.log(
  `The area of a triangle with base ${base} and height ${height} is ${area}`
);
